<UserControl x:Class="ProjektStacjaWPF.Views.StartPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjektStacjaWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="600"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="350"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <Label x:Name="UserInfo" Padding="5" FontSize="24" Margin="20"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="Information" Padding="5" IsReadOnly="True" Background="Transparent" TextWrapping="WrapWithOverflow" BorderThickness="0"/>
        </StackPanel>
                
    </Grid>
</UserControl>
